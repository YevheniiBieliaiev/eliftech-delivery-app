export enum ApiRoutes {
  SHOP = '/shop',
  AUTH = '/auth',
  ORDER = '/order',
  CART = '/cart',
}

export enum ShopRoutes {
  GET_PRODUCTS = '/get-products',
}

export enum AuthRoutes {
  SIGN_UP = '/sign-up',
  SIGN_IN = '/sign-in',
}
